---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-05-26T16:02:58.804Z
provider: 
---

> 本规范定义了在此游戏设计文档仓库中进行创建、修改、审查等操作时必须遵守的规则。所有 AI 辅助操作必须严格遵循本规范。

---

## 一、项目认知

### 1.1 项目性质

- 这是一个**游戏设计文档仓库**，不是代码仓库
- 所有文件均为 Markdown（.md）格式的游戏设计文档
- 核心目标：为「肉鸽旅途2」提供唯一、准确、可追溯的设计真相源

### 1.2 核心设计理念（必须牢记）

| 理念 | 含义 |
|------|------|
| **AP 只管数量，EN 只管质量** | AP=1 是标准出牌消耗，不存在 AP≥2 的牌（极少数强力能力牌除外） |
| **策略前置** | 玩家在战前完成构筑，局内由系统全自动驱动 |
| **规则严苛，遗物破局** | 基础规则无兜底，遗物通过篡改底层规则创造流派 |
| **引擎思维** | 卡组是一个自动运转的引擎，玩家追求稳定性与爆发力的平衡 |

---

## 二、文档架构规范

### 2.1 目录编号体系

| 编号 | 模块 | 含义 |
|------|------|------|
| 00 | 总纲 | 全局性、跨系统文档 |
| 01 | 核心战斗 | 战斗系统及直接相关子系统 |
| 02 | 局外系统 | 非战斗玩法 |
| 03 | 进度与成长 | 长线系统 |
| 04 | 数值规划 | 数值体系 |
| 05 | 体验与表现 | 非机制性设计 |

- 新增模块时顺延编号
- 目录命名格式：`编号_模块名`（如 `02_局外系统`）
- 文件命名：**使用中文**，格式为 `模块名.md`

### 2.2 文档分类

| 类型 | 侧重内容 | 示例 |
|------|---------|------|
| **规范类** | 原则、约束、模板、禁区 | `卡组构筑规范.md` |
| **数据类** | 具体条目、数值、效果描述 | `卡牌设计.md` |
| **机制类** | 规则、流程、边界条件 | `核心战斗机制.md` |

---

## 三、★ 数据单一来源规则（最高优先级）

### 3.1 核心原则

`00_总纲/数据字典.md` 是**所有数值和规则的唯一定义源**。

### 3.2 写入规则

1. **新增数值/规则** → 必须先添加到数据字典，再在其他文档中引用
2. **修改数值/规则** → 只改数据字典，按「影响文档」列逐项检查并更新其他文档
3. **引用数值** → 写"见《数据字典》"或"见《数据字典·XX节》"，**禁止重复写具体数值**

### 3.3 变更检查清单

修改数据字典后，必须按以下清单逐项检查：

| 修改内容 | 必须检查的文档 |
|----------|--------------|
| AP/EN 参数 | 核心战斗机制、卡组构筑规范、卡牌设计、数值框架 |
| 护盾衰减率 | 核心战斗机制、数值框架、术语表、遗物系统、敌人设计 |
| 初始卡组 | 卡组构筑规范、卡牌设计、遗物系统（能量晶体/鲜血圣杯） |
| 锚点数值 | 卡组构筑规范、数值框架、卡牌设计（校验表） |
| 状态效果 | 核心战斗机制、术语表、数值框架 |
| 敌人 HP | 敌人设计、数值框架、术语表、遗物系统 |
| 伤害公式 | 核心战斗机制、数值框架 |

### 3.4 术语规则

- 所有术语以 `00_总纲/术语表.md` 为准
- 新术语首次出现时，**必须**添加到术语表
- 引用术语时必须使用术语表中的标准名称

---

## 四、文档编辑规则

### 4.1 创建新文档

必须包含以下结构（按需取舍）：

```markdown
# 文档标题

> 一句话概述本文档的核心内容。

## 设计目标
- 本系统/模块要解决什么问题？
- 玩家体验目标是什么？

## 核心规则 / 主要内容
- 具体的规则描述或内容

## 与其他系统的关联
- 依赖哪些系统？被哪些系统依赖？

## 待定事项
- [ ] 未决定的设计点

## 变更记录
| 日期 | 变更内容 |
|------|----------|
| YYYY-MM-DD | 初始版本 |
```

### 4.2 修改现有文档

1. **必须先通读目标文档全文**，理解上下文后再修改
2. 修改后**必须更新**文档末尾的「变更记录」表格
3. 如果涉及数据字典中数值的修改，**必须先改数据字典**
4. 修改后必须检查「与其他系统的关联」中列出的上下游文档是否受影响
5. 废弃内容不移除，移入 `归档/` 目录

### 4.3 文档规模控制

- 单文件不超过 **200 行**（超出则拆分为子文件）
- 数据类文档放在对应模块的 `数据/` 子目录下
- 拆分的子文件按逻辑分类，不按字母排序

### 4.4 禁止事项

- ❌ **禁止**在其他文档中重复定义数据字典已有的数值
- ❌ **禁止**未经数据字典更新就修改其他文档的数值
- ❌ **禁止**跳过「变更记录」更新
- ❌ **禁止**使用英文术语替代术语表中的中文标准术语
- ❌ **禁止**在原地删除废弃内容（应移入归档）
- ❌ **禁止**在未理解完整设计逻辑的情况下修改核心战斗机制

---

## 五、设计一致性校验

### 5.1 数值校验

每次修改数值后，按以下维度校验：

| 校验项 | 检查方法 |
|--------|---------|
| AP/EN 分布 | 实际占比是否在目标占比范围内 |
| 伤害/护盾锚点 | 单张牌的伤害/AP 是否等于锚点值（或偏差有合理理由） |
| 稀有度分布 | 实际占比是否在目标占比范围内 |
| 类型分布 | 攻击/技能/能力牌比例是否合理 |
| 敌人伤害基准 | 伤害/回合是否在对应类型的基准值附近 |

### 5.2 规则一致性

- 新增卡牌的效果必须能在「核心战斗机制」中找到对应的规则支持
- 新增遗物的效果必须落在「可篡改的规则维度」表中
- 新增敌人必须符合「数值校验标准」表中的 HP/伤害范围
- 新增状态效果必须先更新「核心战斗机制·状态体系」章节

---

## 六、模块间依赖关系

### 6.1 依赖图

```
00_总纲（术语表 + 数据字典）
    ↓
01_核心战斗（核心战斗机制）
    ├──→ 卡组构筑规范
    │       └──→ 卡牌设计
    ├──→ 遗物系统
    └──→ 敌人设计
    ↓
04_数值规划（数值框架）
    ↑ 互赖 ↑
    ↓       ↓
02_局外系统 ←──→ 03_进度与成长
    ↓
05_体验与表现
```

### 6.2 修改影响传导

- 修改 `核心战斗机制` → 必须检查「卡牌设计」「遗物系统」「敌人设计」「卡组构筑规范」
- 修改 `数值框架` → 必须检查「卡牌设计」「敌人设计」「卡组构筑规范」
- 修改 `卡组构筑规范` → 必须检查「卡牌设计」
- 修改 `术语表` → 全文搜索旧术语名并统一替换

---

## 七、AI 交互规则

### 7.1 回答问题时

- 涉及数值 → 优先查阅数据字典，确认后引用
- 涉及规则 → 优先查阅核心战斗机制
- 涉及术语 → 以术语表为准，不一致时指出
- 不确定时 → 明确标注"待确认"，而非猜测

### 7.2 执行修改时

- 修改前先说明：涉及哪些文件、变更原因、影响范围
- 多文件修改时，按依赖顺序执行
- 修改后主动提示需要校验哪些关联文档

### 7.3 文档分析时

- 关注文档状态标记（✅ 已确定 / 🟡 初版完成 / 🔲 待设计）
- 优先处理 ✅ 和 🟡 级别的内容
- 🔲 级别暂不展开，除非用户明确要求

---

## 八、状态标记规范

| 标记 | 含义 | 使用场景 |
|------|------|---------|
| ✅ 已确定 | 内容已封板，不可随意修改 | 核心机制、数值框架 |
| 🟡 初版完成 | 初版已产出，待验证/补充 | 卡牌设计、敌人设计 |
| 🔲 待设计 | 尚未开始 | 进度与成长、体验与表现 |
| ⚠️ 需关注 | 有已知问题待解决 | 分布偏差、待定参数 |

---

## 九、Git 规范

- 提交信息使用**中文**，格式：`模块名: 变更摘要`
- 示例：`核心战斗: 初始卡组从8张改为10张`
- 涉及数据字典变更时，提交信息前缀为 `数据字典:`
- 不提交临时文件、个人笔记

---

## 十、快速参考卡

| 场景 | 第一步 |
|------|--------|
| 新增卡牌 | 查阅数据字典锚点 → 查阅卡组构筑规范 AP/EN 分布 → 写入卡牌设计 → 更新校验表 |
| 新增遗物 | 确认规则篡改维度 → 查阅遗物系统设计约束 → 写入遗物系统 → 更新流派表 |
| 新增敌人 | 查阅数值校验标准 → 参照现有敌人结构 → 写入敌人设计 |
| 修改数值 | **先改数据字典** → 按影响文档清单逐项更新 → 校验一致性 |
| 新增术语 | 写入术语表 → 在其他文档中统一使用新术语 |
| 新增模块 | 顺延编号 → 创建目录 → 按模板写文档 → 更新 README 导航 |